#!/usr/bin/env bash
# Thin shim: delegates to beskar_tools.cli.get_abs_token.

set -euo pipefail

SCRIPT_PATH="${BASH_SOURCE[0]}"
TOOLS_DIR="$(cd "$(dirname "$SCRIPT_PATH")" && pwd)"

exec env BESKAR_SHIM_CALLER="$SCRIPT_PATH" "$TOOLS_DIR/_shim.sh" beskar_tools.cli.get_abs_token "$@"
